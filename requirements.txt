# Shared dependencies for evaluate.py and the paper experiments.
# Compatibility bounds, not a frozen environment for the published runs.

# Model inference (GPU)
torch>=2.1.0
transformers>=4.46.0
vllm>=0.6.0
accelerate>=0.30.0
huggingface_hub>=0.23.0
# flash-attn is optional; install separately if your CUDA/torch combination supports it.

# Data
datasets>=2.19.0
numpy>=1.26.0
pandas>=2.2.0

# Probes and statistics
scikit-learn>=1.4.0
scipy>=1.12.0
statsmodels>=0.14.0

# LLM judge
anthropic>=0.30.0

# Plots
matplotlib>=3.8.0
seaborn>=0.13.0
adjustText>=1.0.0

tqdm>=4.66.0
